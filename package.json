{
  "name": "apperal_module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "test:allure": "playwright test --project=chromium; npm run allure:generate && npm run allure:open",
    "test:allure:headed": "playwright test --project=chromium --headed; npm run allure:generate && npm run allure:open",
    "tn": "playwright test --project=chromium && npm run allure:generate && npm run allure:open"
  },  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.7.0",
    "@types/xlsx": "^0.0.35",
    "allure-commandline": "^2.41.0",
    "allure-playwright": "^3.8.0",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
